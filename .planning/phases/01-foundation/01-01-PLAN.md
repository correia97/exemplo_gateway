---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - OpenCode.sln
  - src/OpenCode.AppHost/OpenCode.AppHost.csproj
  - src/OpenCode.AppHost/Program.cs
  - src/OpenCode.AppHost/Properties/launchSettings.json
  - src/OpenCode.AppHost/appsettings.json
  - src/OpenCode.ServiceDefaults/OpenCode.ServiceDefaults.csproj
  - src/OpenCode.ServiceDefaults/Extensions.cs
  - src/OpenCode.DragonBall.Api/OpenCode.DragonBall.Api.csproj
  - src/OpenCode.DragonBall.Api/Program.cs
  - src/OpenCode.DragonBall.Api/Properties/launchSettings.json
  - src/OpenCode.DragonBall.Api/appsettings.json
  - src/OpenCode.Music.Api/OpenCode.Music.Api.csproj
  - src/OpenCode.Music.Api/Program.cs
  - src/OpenCode.Music.Api/Properties/launchSettings.json
  - src/OpenCode.Music.Api/appsettings.json
autonomous: true
requirements:
  - INFRA-03
  - INFRA-05
  - DBALL-01
  - MUSIC-01
must_haves:
  truths:
    - "Developer can run `dotnet build` on the entire solution"
    - "Solution file exists with all 4 projects referenced"
    - "Both API projects reference ServiceDefaults project"
    - "All project directories exist with correct template structure"
  artifacts:
    - path: "OpenCode.sln"
      provides: "Solution file referencing all projects"
      min_lines: 5
    - path: "src/OpenCode.AppHost/Program.cs"
      provides: "Aspire AppHost entry point"
      min_lines: 3
    - path: "src/OpenCode.ServiceDefaults/Extensions.cs"
      provides: "Service defaults extension methods"
      min_lines: 10
    - path: "src/OpenCode.DragonBall.Api/Program.cs"
      provides: "DragonBall API entry point"
      min_lines: 3
    - path: "src/OpenCode.Music.Api/Program.cs"
      provides: "Music API entry point"
      min_lines: 3
  key_links:
    - from: "OpenCode.sln"
      to: "src/OpenCode.AppHost/OpenCode.AppHost.csproj"
      via: "dotnet sln add"
      pattern: "OpenCode.AppHost"
    - from: "OpenCode.sln"
      to: "src/OpenCode.ServiceDefaults/OpenCode.ServiceDefaults.csproj"
      via: "dotnet sln add"
      pattern: "OpenCode.ServiceDefaults"
    - from: "OpenCode.sln"
      to: "src/OpenCode.DragonBall.Api/OpenCode.DragonBall.Api.csproj"
      via: "dotnet sln add"
      pattern: "DragonBall.Api"
    - from: "OpenCode.sln"
      to: "src/OpenCode.Music.Api/OpenCode.Music.Api.csproj"
      via: "dotnet sln add"
      pattern: "Music.Api"
    - from: "OpenCode.DragonBall.Api.csproj"
      to: "OpenCode.ServiceDefaults.csproj"
      via: "ProjectReference"
      pattern: "ServiceDefaults"
    - from: "OpenCode.Music.Api.csproj"
      to: "OpenCode.ServiceDefaults.csproj"
      via: "ProjectReference"
      pattern: "ServiceDefaults"
---

<objective>
Scaffold the entire .NET 10 solution skeleton — the foundational structure every subsequent phase plugs into.

Purpose: Create the solution file, all 4 project directories (AppHost, ServiceDefaults, DragonBall.Api, Music.Api) with their default template files, wire project references, and verify `dotnet build` succeeds.

Output: A compilable .NET 10 solution at `D:\projetos\opencodeDeepseek\` with OpenCode.sln, src/OpenCode.AppHost/, src/OpenCode.ServiceDefaults/, src/OpenCode.DragonBall.Api/, src/OpenCode.Music.Api/
</objective>

<execution_context>
@D:/projetos/opencodeDeepseek/.opencode/get-shit-done/workflows/execute-plan.md
@D:/projetos/opencodeDeepseek/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
</context>

<interfaces>
<!-- Greenfield project — no existing codebase interfaces to extract.
All project structures come from .NET 10 SDK templates:

- aspire-apphost: Creates Program.cs with DistributedApplication.CreateBuilder()
- aspire-servicedefaults: Creates Extensions.cs with AddServiceDefaults()
- webapi: Creates Program.cs with WebApplication.CreateBuilder()

Key template artifacts created by dotnet new:
- AppHost: Program.cs with builder.AddProject<Projects.*>() pattern, csproj with Aspire.Hosting.AppHost
- ServiceDefaults: Extensions.cs with AddServiceDefaults(), csproj with OpenTelemetry packages
- webapi: Program.cs with builder.Services.AddEndpointsApiExplorer(), csproj with ASP.NET Core packages
-->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create solution and scaffold all 4 projects from templates</name>
  <files>OpenCode.sln, src/OpenCode.AppHost/ (directory), src/OpenCode.ServiceDefaults/ (directory), src/OpenCode.DragonBall.Api/ (directory), src/OpenCode.Music.Api/ (directory)</files>
  <read_first>
    D:\projetos\opencodeDeepseek\.planning\research\ARCHITECTURE.md (project structure section)
  </read_first>
  <action>
    Use dotnet CLI to scaffold the entire solution. Run commands from `D:\projetos\opencodeDeepseek\`. Verify the .NET 10 SDK is installed first:

    1. Create solution:
    ```bash
    dotnet new sln -n OpenCode
    ```

    2. Create Aspire AppHost project:
    ```bash
    dotnet new aspire-apphost -n OpenCode.AppHost -o src/OpenCode.AppHost
    ```

    3. Create ServiceDefaults project:
    ```bash
    dotnet new aspire-servicedefaults -n OpenCode.ServiceDefaults -o src/OpenCode.ServiceDefaults
    ```

    4. Create DragonBall API (Minimal API):
    ```bash
    dotnet new webapi -n OpenCode.DragonBall.Api -o src/OpenCode.DragonBall.Api --use-minimal-apis true
    ```

    5. Create Music API (Minimal API):
    ```bash
    dotnet new webapi -n OpenCode.Music.Api -o src/OpenCode.Music.Api --use-minimal-apis true
    ```

    6. Add all projects to solution:
    ```bash
    dotnet sln OpenCode.sln add src/OpenCode.AppHost/OpenCode.AppHost.csproj
    dotnet sln OpenCode.sln add src/OpenCode.ServiceDefaults/OpenCode.ServiceDefaults.csproj
    dotnet sln OpenCode.sln add src/OpenCode.DragonBall.Api/OpenCode.DragonBall.Api.csproj
    dotnet sln OpenCode.sln add src/OpenCode.Music.Api/OpenCode.Music.Api.csproj
    ```

    7. Add ServiceDefaults project reference to both API projects:
    ```bash
    dotnet add src/OpenCode.DragonBall.Api/OpenCode.DragonBall.Api.csproj reference src/OpenCode.ServiceDefaults/OpenCode.ServiceDefaults.csproj
    dotnet add src/OpenCode.Music.Api/OpenCode.Music.Api.csproj reference src/OpenCode.ServiceDefaults/OpenCode.ServiceDefaults.csproj
    ```

    IMPORTANT: Do NOT modify any template-generated files yet (Program.cs, Extensions.cs, csproj). The next plans handle those modifications. Keep the template defaults for now.
  </action>
  <verify>
    <automated>dotnet build OpenCode.sln 2>&1 | Select-String -Pattern "Build succeeded" -Quiet</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build OpenCode.sln` exits with code 0
    - `OpenCode.sln` exists at solution root and contains 4 project entries
    - Directory `src/OpenCode.AppHost/` exists with Program.cs and csproj
    - Directory `src/OpenCode.ServiceDefaults/` exists with Extensions.cs and csproj
    - Directory `src/OpenCode.DragonBall.Api/` exists with Program.cs and csproj
    - Directory `src/OpenCode.Music.Api/` exists with Program.cs and csproj
    - `grep "ServiceDefaults" src/OpenCode.DragonBall.Api/*.csproj` returns a ProjectReference line
    - `grep "ServiceDefaults" src/OpenCode.Music.Api/*.csproj` returns a ProjectReference line
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Project Reference Boundary | Project references between ServiceDefaults and API projects — no external trust boundary at Phase 1 scope |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | T (Tampering) | Project structure files | accept | Phase 1 creates only scaffolding — no runtime data flows yet. Tampering risk is limited to developer workstation and mitigated by version control. |
| T-01-02 | S (Spoofing) | N/A | accept | No authentication implemented yet. Authentication comes in Phase 4 (Keycloak). |
</threat_model>

<verification>
1. The solution builds: `dotnet build OpenCode.sln` returns "Build succeeded"
2. All 4 projects are referenced in the solution: `dotnet sln list` shows 4 projects
3. Both API csproj files contain a ProjectReference to ServiceDefaults
4. Template files remain unmodified (ready for Plans 01-02, 01-03, 01-04)
</verification>

<success_criteria>
- `dotnet build OpenCode.sln` succeeds with zero errors
- Solution contains 4 projects: AppHost, ServiceDefaults, DragonBall.Api, Music.Api
- Both API projects reference ServiceDefaults
- All project directories have standard template structure
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
