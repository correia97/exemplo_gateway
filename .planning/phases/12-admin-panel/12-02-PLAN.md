---
phase: 12-admin-panel
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/OpenCode.AngularFrontend/src/api/admin.service.ts
  - src/OpenCode.AngularFrontend/src/shared/components/admin-table/admin-table.component.ts
  - src/OpenCode.AngularFrontend/src/shared/components/admin-table/admin-table.component.html
  - src/OpenCode.AngularFrontend/src/shared/components/admin-table/admin-table.component.css
  - src/OpenCode.AngularFrontend/src/shared/components/confirm-dialog/confirm-dialog.component.ts
  - src/OpenCode.AngularFrontend/src/shared/components/confirm-dialog/confirm-dialog.component.html
  - src/OpenCode.AngularFrontend/src/shared/components/confirm-dialog/confirm-dialog.component.css
  - src/OpenCode.AngularFrontend/src/pages/admin/admin-layout.component.ts
  - src/OpenCode.AngularFrontend/src/pages/admin/admin-layout.component.html
  - src/OpenCode.AngularFrontend/src/pages/admin/admin-layout.component.css
  - src/OpenCode.AngularFrontend/src/pages/admin/dashboard/dashboard.component.ts
  - src/OpenCode.AngularFrontend/src/pages/admin/dashboard/dashboard.component.html
  - src/OpenCode.AngularFrontend/src/pages/admin/characters/characters.component.ts
  - src/OpenCode.AngularFrontend/src/pages/admin/characters/characters.component.html
  - src/OpenCode.AngularFrontend/src/pages/admin/genres/genres.component.ts
  - src/OpenCode.AngularFrontend/src/pages/admin/genres/genres.component.html
  - src/OpenCode.AngularFrontend/src/pages/admin/artists/artists.component.ts
  - src/OpenCode.AngularFrontend/src/pages/admin/artists/artists.component.html
  - src/OpenCode.AngularFrontend/src/pages/admin/albums/albums.component.ts
  - src/OpenCode.AngularFrontend/src/pages/admin/albums/albums.component.html
  - src/OpenCode.AngularFrontend/src/pages/admin/tracks/tracks.component.ts
  - src/OpenCode.AngularFrontend/src/pages/admin/tracks/tracks.component.html
  - src/OpenCode.AngularFrontend/src/app/app.routes.ts
autonomous: true
requirements:
  - ADMIN-01
  - ADMIN-02
  - ADMIN-03
  - ADMIN-04
  - ADMIN-08
must_haves:
  truths:
    - "AdminLayoutComponent renders with left sidebar navigation organized by domain (Dashboard / Dragon Ball / Music)"
    - "AdminTableComponent supports search input, sortable column headers, and Edit/Delete action buttons"
    - "ConfirmDialogComponent shows entity type, entity name, and requires two-step confirmation before delete"
    - "DashboardComponent shows stat cards for Characters, Genres, Artists, Albums, Tracks via pageSize=1 trick"
    - "CharactersComponent lists characters with search, edit, delete confirmation — uses existing /api/characters CRUD"
    - "GenresComponent lists genres with search, edit, delete — uses existing /api/genres CRUD"
    - "ArtistsComponent lists artists with search, edit, delete — uses existing /api/artists CRUD"
    - "AlbumsComponent lists albums with search, edit, delete — uses existing /api/albums CRUD"
    - "TracksComponent lists tracks with search, edit, delete — uses existing /api/tracks CRUD"
    - "Admin routes registered under /admin with RoleGuard requiring 'editor' role"
    - "Non-editor users redirected away from /admin routes"
    - "Entity tables show CreatedAt and UpdatedAt columns for dates-only audit per D-06"
  artifacts:
    - path: "src/OpenCode.AngularFrontend/src/api/admin.service.ts"
      provides: "Injectable Angular service with fetchStats() methods using pageSize=1 trick"
      min_lines: 40
    - path: "src/OpenCode.AngularFrontend/src/shared/components/admin-table/"
      provides: "Standalone Angular component: @Input columns/data, @Output edit/delete, search input, sortable headers, pagination"
    - path: "src/OpenCode.AngularFrontend/src/shared/components/confirm-dialog/"
      provides: "Standalone Angular component: modal with entity type/name, @Output confirm/cancel, loading state"
    - path: "src/OpenCode.AngularFrontend/src/pages/admin/admin-layout.component.ts"
      provides: "Admin shell with dark sidebar, RouterLink items per domain, <router-outlet> for nested routes"
    - path: "src/OpenCode.AngularFrontend/src/pages/admin/dashboard/"
      provides: "Dashboard component with stat cards for 5 entity types"
  key_links:
    - from: "app.routes.ts"
      to: "admin-layout.component.ts"
      via: "Route path: 'admin' with loadComponent + canActivate: [RoleGuard]"
      pattern: "admin-layout"
    - from: "admin-layout.component.html"
      to: "entity admin components"
      via: "routerLink to /admin/characters, /admin/genres, etc."
      pattern: "routerLink.*admin"
    - from: "admin.service.ts"
      to: "existing CRUD endpoints"
      via: "fetch(DRAGONBALL_API_URL/api/characters?pageSize=1)"
      pattern: "pageSize=1"
---

<objective>
Build the complete Angular admin panel with feature parity to the React admin panel: admin layout with sidebar navigation, enhanced AdminTableComponent (search/sort/action buttons), ConfirmDialogComponent for safe deletes, dashboard with entity counts from existing API endpoints, and admin CRUD components for Characters, Genres, Artists, Albums, and Tracks. All operations use existing CRUD endpoints — no backend changes.

Purpose: Deliver the Angular side of the admin panel matching the React implementation feature-for-feature (ADMIN-08). Every admin page, shared component, and route guard must behave identically in both frontends.

Output:
- api/admin.service.ts — Injectable admin API service for dashboard stats
- shared/components/admin-table/ — AdminTableComponent with search, sort, actions
- shared/components/confirm-dialog/ — ConfirmDialogComponent for delete safety
- pages/admin/admin-layout.component — Admin shell with sidebar navigation
- pages/admin/dashboard/ — DashboardComponent with entity count stat cards
- pages/admin/characters/ — Characters CRUD component
- pages/admin/genres/ — Genres CRUD component
- pages/admin/artists/ — Artists CRUD component
- pages/admin/albums/ — Albums CRUD component
- pages/admin/tracks/ — Tracks CRUD component
- Updated app.routes.ts — Admin routes with RoleGuard requiring editor role
</objective>

<execution_context>
@D:/projetos/opencodeDeepseek/.opencode/get-shit-done/workflows/execute-plan.md
@D:/projetos/opencodeDeepseek/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-admin-panel/12-CONTEXT.md
@.planning/phases/12-admin-panel/12-01-PLAN.md

<interfaces>
Existing Angular patterns the admin components must follow:

From src/OpenCode.AngularFrontend/src/shared/components/data-table/data-table.ts:
```typescript
export interface Column<T> {
  key: string; label: string; render?: (item: T) => string;
}

@Component({ selector: 'app-data-table', standalone: true, ... })
export class DataTableComponent<T extends { [key: string]: any }> {
  @Input() columns: Column<T>[] = [];
  @Input() data: T[] = [];
  @Input() keyExtractor: (item: T) => string | number = () => 0;
  @Input() isLoading = false;
  @Input() rowClickable = false;
  @Output() rowClick = new EventEmitter<T>();
}
```

From src/OpenCode.AngularFrontend/src/shared/components/data-table/data-table.html:
```html
@if (isLoading) { <div>Loading...</div> }
@else {
  <table> <thead> @for (col of columns; track col.key) { <th>{{ col.label }}</th> } </thead>
    <tbody> @for (item of data; track keyExtractor(item)) {
      <tr> @for (col of columns; track col.key) {
        <td>{{ col.render ? col.render(item) : item[col.key] ?? '' }}</td>
      }</tr>
    }</tbody>
  </table>
}
```

Existing Angular Layout component pattern (from shared/components/layout/):
```typescript
@Component({ selector: 'app-layout', standalone: true, imports: [RouterLink, RouterOutlet, AsyncPipe], ... })
export class LayoutComponent {
  private auth = inject(AuthService);
  isAuthenticated$ = this.auth.isAuthenticated$;
  userData$ = this.auth.userData$;
  sidebarOpen = true;
}
```

Angular route pattern (from app.routes.ts):
```typescript
export const routes: Routes = [
  { path: '', loadComponent: () => import('../pages/dashboard/dashboard').then(m => m.DashboardComponent) },
  { path: 'dragonball', loadChildren: () => import('../pages/dragonball/dragonball.routes').then(m => m.routes) },
  { path: '**', redirectTo: '', pathMatch: 'full' },
];
```

Existing RoleGuard (from auth/role.guard.ts):
```typescript
@Injectable({ providedIn: 'root' })
export class RoleGuard implements CanActivate {
  private auth = inject(AuthService);
  canActivate(route: any): Observable<boolean | UrlTree> {
    const requiredRoles: string[] = route.data?.['roles'] ?? [];
    // Redirects to home if user lacks required role
    return this.auth.getRoles().pipe(
      map(userRoles => requiredRoles.some(r => userRoles.includes(r)) || this.router.parseUrl('/'))
    );
  }
}
```

Existing AuthService methods (from auth/auth.service.ts):
```typescript
getRoles(): Observable<string[]>  // Returns realm roles including 'editor'
hasRole(role: string): Observable<boolean>
```

Existing DragonBall API service pattern (from pages/dragonball/* - using direct fetch or injected services):
Characters use existing CRUD endpoints at `DRAGONBALL_API_URL/api/characters/*`
Music entities use `MUSIC_API_URL/api/genres/*`, `/api/artists/*`, `/api/albums/*`, `/api/tracks/*`

Existing API URL configuration pattern (from src/OpenCode.AngularFrontend/src/environments/):
The Angular app uses environment files for API URLs (typically `DRAGONBALL_API_URL` and `MUSIC_API_URL`).
</interfaces>
</context>

<tasks>

<task type="auto">
<name>Task 1: Create Angular admin API service</name>
<files>
  src/OpenCode.AngularFrontend/src/api/admin.service.ts
</files>
<read_first>
  src/OpenCode.AngularFrontend/src/api/ (list directory)
  src/OpenCode.AngularFrontend/src/auth/auth.service.ts
</read_first>
<action>
Create `src/OpenCode.AngularFrontend/src/api/admin.service.ts` — an injectable Angular service that provides dashboard stats by calling existing list endpoints with pageSize=1.

The service uses Angular's HttpClient (already configured in the app). Create the directory if it doesn't exist:

```typescript
import { Injectable, inject } from '@angular/core';
import { HttpClient } from '@angular/common/http';
import { Observable, forkJoin, map } from 'rxjs';

export interface AdminStats {
  characters: number;
  genres: number;
  artists: number;
  albums: number;
  tracks: number;
}

@Injectable({ providedIn: 'root' })
export class AdminService {
  private http = inject(HttpClient);

  /**
   * Fetch all entity counts by calling existing list endpoints with pageSize=1.
   * Reads totalCount from the paginated response. No dedicated stats endpoint.
   */
  fetchStats(): Observable<AdminStats> {
    const baseUrl = 'http://localhost:8000/api'; // through Kong gateway
    const pageSize1 = '?pageSize=1';

    const characters$ = this.http.get<any>(`${baseUrl}/characters${pageSize1}`)
      .pipe(map(r => r.totalCount ?? 0));
    const genres$ = this.http.get<any>(`${baseUrl}/genres${pageSize1}`)
      .pipe(map(r => r.totalCount ?? 0));
    const artists$ = this.http.get<any>(`${baseUrl}/artists${pageSize1}`)
      .pipe(map(r => r.totalCount ?? 0));
    const albums$ = this.http.get<any>(`${baseUrl}/albums${pageSize1}`)
      .pipe(map(r => r.totalCount ?? 0));
    const tracks$ = this.http.get<any>(`${baseUrl}/tracks${pageSize1}`)
      .pipe(map(r => r.totalCount ?? 0));

    return forkJoin({
      characters: characters$,
      genres: genres$,
      artists: artists$,
      albums: albums$,
      tracks: tracks$,
    });
  }
}
```
</action>
<verify>
  <automated>python3 -c "
with open('src/OpenCode.AngularFrontend/src/api/admin.service.ts') as f:
    c = f.read()
assert 'AdminService' in c
assert 'fetchStats' in c
assert 'pageSize=1' in c
assert 'AdminStats' in c
assert 'providedIn: \"root\"' in c or \"providedIn: 'root'\" in c
assert 'forkJoin' in c
print('AdminService created')
"</automated>
</verify>
<done>
AdminService with fetchStats() method using pageSize=1 trick created
</done>
</task>

<task type="auto">
<name>Task 2: Create Angular admin shared components</name>
<files>
  src/OpenCode.AngularFrontend/src/shared/components/admin-table/admin-table.component.ts
  src/OpenCode.AngularFrontend/src/shared/components/admin-table/admin-table.component.html
  src/OpenCode.AngularFrontend/src/shared/components/admin-table/admin-table.component.css
  src/OpenCode.AngularFrontend/src/shared/components/confirm-dialog/confirm-dialog.component.ts
  src/OpenCode.AngularFrontend/src/shared/components/confirm-dialog/confirm-dialog.component.html
  src/OpenCode.AngularFrontend/src/shared/components/confirm-dialog/confirm-dialog.component.css
</files>
<read_first>
  src/OpenCode.AngularFrontend/src/shared/components/data-table/data-table.ts
  src/OpenCode.AngularFrontend/src/shared/components/data-table/data-table.html
  src/OpenCode.AngularFrontend/src/shared/components/pagination/pagination.ts
</read_first>
<action>
Create admin shared components directory structure and files:

**File: src/OpenCode.AngularFrontend/src/shared/components/admin-table/admin-table.component.ts**
```typescript
import { Component, Input, Output, EventEmitter } from '@angular/core';
import { NgFor } from '@angular/common';
import { FormsModule } from '@angular/forms';
import { PaginationComponent } from '../pagination/pagination';

export interface AdminColumn {
  key: string;
  label: string;
  sortable?: boolean;
  render?: (item: any) => string;
}

@Component({
  selector: 'app-admin-table',
  standalone: true,
  imports: [NgFor, FormsModule, PaginationComponent],
  templateUrl: './admin-table.component.html',
  styleUrl: './admin-table.component.css',
})
export class AdminTableComponent {
  @Input() columns: AdminColumn[] = [];
  @Input() data: any[] = [];
  @Input() keyExtractor: (item: any) => string | number = () => 0;
  @Input() isLoading = false;
  @Input() page = 1;
  @Input() totalPages = 1;
  @Input() totalCount = 0;
  @Output() edit = new EventEmitter<any>();
  @Output() delete = new EventEmitter<any>();
  @Output() pageChange = new EventEmitter<number>();

  search = '';
  sortKey: string | null = null;
  sortDir: 'asc' | 'desc' = 'asc';

  get filteredData(): any[] {
    if (!this.search) return this.data;
    const q = this.search.toLowerCase();
    return this.data.filter((item: any) =>
      this.columns.some(col => {
        const val = item[col.key];
        return val != null && String(val).toLowerCase().includes(q);
      })
    );
  }

  get sortedData(): any[] {
    if (!this.sortKey) return this.filteredData;
    return [...this.filteredData].sort((a: any, b: any) => {
      const aVal = a[this.sortKey!];
      const bVal = b[this.sortKey!];
      if (aVal == null) return 1;
      if (bVal == null) return -1;
      const cmp = String(aVal).localeCompare(String(bVal));
      return this.sortDir === 'asc' ? cmp : -cmp;
    });
  }

  toggleSort(key: string): void {
    if (this.sortKey === key) {
      this.sortDir = this.sortDir === 'asc' ? 'desc' : 'asc';
    } else {
      this.sortKey = key;
      this.sortDir = 'asc';
    }
  }
}
```

**File: src/OpenCode.AngularFrontend/src/shared/components/admin-table/admin-table.component.html**
```html
@if (isLoading) {
  <div class="p-8 text-center text-gray-400">Loading...</div>
} @else {
  <div class="mb-4">
    <input type="text" [(ngModel)]="search" placeholder="Search..."
      class="w-full p-2 border rounded text-sm" />
  </div>
  <table class="w-full border-collapse">
    <thead>
      <tr class="bg-gray-100 text-left">
        @for (col of columns; track col.key) {
          <th class="p-3 border-b-2 border-gray-300 text-sm font-semibold"
            [class.cursor-pointer]="col.sortable"
            [class.hover:bg-gray-200]="col.sortable"
            (click)="col.sortable && toggleSort(col.key)">
            {{ col.label }}
            @if (sortKey === col.key) {
              <span>{{ sortDir === 'asc' ? ' ▲' : ' ▼' }}</span>
            }
          </th>
        }
        <th class="p-3 border-b-2 border-gray-300 text-sm font-semibold">Actions</th>
      </tr>
    </thead>
    <tbody>
      @for (item of sortedData; track keyExtractor(item)) {
        <tr class="border-b border-gray-200 hover:bg-gray-50">
          @for (col of columns; track col.key) {
            <td class="p-2.5 px-3 text-sm">
              {{ col.render ? col.render(item) : item[col.key] ?? '' }}
            </td>
          }
          <td class="p-2.5 px-3">
            <div class="flex gap-2">
              <button (click)="edit.emit(item)"
                class="px-3 py-1 bg-blue-500 text-white rounded text-sm hover:bg-blue-600">
                Edit
              </button>
              <button (click)="delete.emit(item)"
                class="px-3 py-1 bg-red-500 text-white rounded text-sm hover:bg-red-600">
                Delete
              </button>
            </div>
          </td>
        </tr>
      }
    </tbody>
  </table>
  <div class="mt-4">
    <app-pagination [page]="page" [totalPages]="totalPages" (pageChange)="pageChange.emit($event)"></app-pagination>
  </div>
}
```

**File: src/OpenCode.AngularFrontend/src/shared/components/admin-table/admin-table.component.css**
```css
:host { display: block; }
```

**File: src/OpenCode.AngularFrontend/src/shared/components/confirm-dialog/confirm-dialog.component.ts**
```typescript
import { Component, Input, Output, EventEmitter } from '@angular/core';

@Component({
  selector: 'app-confirm-dialog',
  standalone: true,
  imports: [],
  templateUrl: './confirm-dialog.component.html',
  styleUrl: './confirm-dialog.component.css',
})
export class ConfirmDialogComponent {
  @Input() isOpen = false;
  @Input() title = '';
  @Input() message = '';
  @Input() entityName = '';
  @Input() entityType = '';
  @Input() isLoading = false;
  @Output() confirm = new EventEmitter<void>();
  @Output() cancel = new EventEmitter<void>();
}
```

**File: src/OpenCode.AngularFrontend/src/shared/components/confirm-dialog/confirm-dialog.component.html**
```html
@if (isOpen) {
  <div class="fixed inset-0 z-50 flex items-center justify-center bg-black/50">
    <div class="bg-white rounded-lg shadow-xl p-6 max-w-md w-full">
      <h3 class="text-lg font-bold text-red-600 mb-2">{{ title }}</h3>
      <p class="text-gray-600 mb-4">{{ message }}</p>
      <div class="bg-gray-50 rounded p-3 mb-4">
        <p><strong>Type:</strong> {{ entityType }}</p>
        <p><strong>Name:</strong> {{ entityName }}</p>
      </div>
      <div class="flex justify-end gap-3">
        <button (click)="cancel.emit()" [disabled]="isLoading"
          class="px-4 py-2 bg-gray-200 rounded hover:bg-gray-300">
          Cancel
        </button>
        <button (click)="confirm.emit()" [disabled]="isLoading"
          class="px-4 py-2 bg-red-600 text-white rounded hover:bg-red-700 disabled:opacity-50">
          {{ isLoading ? 'Deleting...' : 'Delete' }}
        </button>
      </div>
    </div>
  </div>
}
```

**File: src/OpenCode.AngularFrontend/src/shared/components/confirm-dialog/confirm-dialog.component.css**
```css
:host { display: contents; }
```

Update the PaginationComponent import path — ensure it's the existing one. The existing pagination is at `src/OpenCode.AngularFrontend/src/shared/components/pagination/pagination.ts`. Verify this file exists before running.
</action>
<verify>
  <automated>python3 -c "
import os
files = [
  'src/OpenCode.AngularFrontend/src/shared/components/admin-table/admin-table.component.ts',
  'src/OpenCode.AngularFrontend/src/shared/components/admin-table/admin-table.component.html',
  'src/OpenCode.AngularFrontend/src/shared/components/confirm-dialog/confirm-dialog.component.ts',
  'src/OpenCode.AngularFrontend/src/shared/components/confirm-dialog/confirm-dialog.component.html',
]
for f in files:
    assert os.path.exists(f), f'{f} does not exist'
with open('src/OpenCode.AngularFrontend/src/shared/components/admin-table/admin-table.component.ts') as f:
    c = f.read()
assert 'AdminTableComponent' in c
assert 'toggleSort' in c and 'search' in c
assert 'edit' in c and 'delete' in c
assert 'PaginationComponent' in c
with open('src/OpenCode.AngularFrontend/src/shared/components/confirm-dialog/confirm-dialog.component.ts') as f:
    c = f.read()
assert 'ConfirmDialogComponent' in c
assert 'confirm' in c and 'cancel' in c and 'isLoading' in c
print('Admin shared components created')
"</automated>
</verify>
<done>
- AdminTableComponent with search input, sortable headers, Edit/Delete action buttons, pagination
- ConfirmDialogComponent with entity info display, confirm/cancel events, loading state
- Both components are standalone and follow existing Angular patterns
</done>
</task>

<task type="auto">
<name>Task 3: Create Angular admin layout and dashboard component</name>
<files>
  src/OpenCode.AngularFrontend/src/pages/admin/admin-layout.component.ts
  src/OpenCode.AngularFrontend/src/pages/admin/admin-layout.component.html
  src/OpenCode.AngularFrontend/src/pages/admin/admin-layout.component.css
  src/OpenCode.AngularFrontend/src/pages/admin/dashboard/dashboard.component.ts
  src/OpenCode.AngularFrontend/src/pages/admin/dashboard/dashboard.component.html
</files>
<read_first>
  src/OpenCode.AngularFrontend/src/shared/components/layout/layout.ts
  src/OpenCode.AngularFrontend/src/shared/components/layout/layout.html
  src/OpenCode.AngularFrontend/src/auth/auth.service.ts
  src/OpenCode.AngularFrontend/src/api/admin.service.ts
</read_first>
<action>
Create the admin layout shell and dashboard component:

**File: src/OpenCode.AngularFrontend/src/pages/admin/admin-layout.component.ts**
```typescript
import { Component } from '@angular/core';
import { RouterLink, RouterLinkActive, RouterOutlet } from '@angular/router';

@Component({
  selector: 'app-admin-layout',
  standalone: true,
  imports: [RouterLink, RouterLinkActive, RouterOutlet],
  templateUrl: './admin-layout.component.html',
  styleUrl: './admin-layout.component.css',
})
export class AdminLayoutComponent {
  navItems: ({ label: string; path?: string; section?: boolean })[] = [
    { label: 'Dashboard', path: '/admin' },
    { label: 'Dragon Ball', section: true },
    { label: 'Characters', path: '/admin/characters' },
    { label: 'Music', section: true },
    { label: 'Genres', path: '/admin/genres' },
    { label: 'Artists', path: '/admin/artists' },
    { label: 'Albums', path: '/admin/albums' },
    { label: 'Tracks', path: '/admin/tracks' },
  ];
}
```

**File: src/OpenCode.AngularFrontend/src/pages/admin/admin-layout.component.html**
```html
<div class="flex min-h-[calc(100vh-4rem)]">
  <aside class="w-64 bg-gray-900 text-white p-4 shrink-0">
    <h2 class="text-lg font-bold mb-4 px-2">Admin Panel</h2>
    <nav class="space-y-1">
      @for (item of navItems; track item.label) {
        @if (item.section) {
          <div class="text-xs uppercase tracking-wider text-gray-400 mt-4 mb-1 px-2">
            {{ item.label }}
          </div>
        } @else {
          <a [routerLink]="item.path" routerLinkActive="bg-blue-600 text-white"
            class="block px-3 py-2 rounded text-sm text-gray-300 hover:bg-gray-800 transition-colors">
            {{ item.label }}
          </a>
        }
      }
    </nav>
  </aside>
  <main class="flex-1 p-6 bg-gray-50">
    <router-outlet />
  </main>
</div>
```

**File: src/OpenCode.AngularFrontend/src/pages/admin/admin-layout.component.css**
```css
:host { display: contents; }
```

**File: src/OpenCode.AngularFrontend/src/pages/admin/dashboard/dashboard.component.ts**
```typescript
import { Component, inject } from '@angular/core';
import { AsyncPipe } from '@angular/common';
import { RouterLink } from '@angular/router';
import { AdminService, AdminStats } from '../../../api/admin.service';

@Component({
  selector: 'app-admin-dashboard',
  standalone: true,
  imports: [AsyncPipe, RouterLink],
  templateUrl: './dashboard.component.html',
})
export class DashboardComponent {
  private adminService = inject(AdminService);
  stats$ = this.adminService.fetchStats();

  cards = [
    { label: 'Characters', key: 'characters', path: '/admin/characters', color: 'bg-blue-500' },
    { label: 'Genres', key: 'genres', path: '/admin/genres', color: 'bg-purple-500' },
    { label: 'Artists', key: 'artists', path: '/admin/artists', color: 'bg-indigo-500' },
    { label: 'Albums', key: 'albums', path: '/admin/albums', color: 'bg-pink-500' },
    { label: 'Tracks', key: 'tracks', path: '/admin/tracks', color: 'bg-teal-500' },
  ];
}
```

**File: src/OpenCode.AngularFrontend/src/pages/admin/dashboard/dashboard.component.html**
```html
<h1 class="text-2xl font-bold mb-6">Admin Dashboard</h1>
@if (stats$ | async; as stats) {
  <div class="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-5 gap-4">
    @for (card of cards; track card.key) {
      <a [routerLink]="card.path"
        class="{{ card.color }} rounded-lg p-4 text-white hover:opacity-90 transition-opacity block">
        <div class="text-3xl font-bold">{{ stats[card.key as keyof AdminStats] }}</div>
        <div class="text-sm opacity-90 mt-1">{{ card.label }}</div>
      </a>
    }
  </div>
}
```
</action>
<verify>
  <automated>python3 -c "
import os
files = [
  'src/OpenCode.AngularFrontend/src/pages/admin/admin-layout.component.ts',
  'src/OpenCode.AngularFrontend/src/pages/admin/admin-layout.component.html',
  'src/OpenCode.AngularFrontend/src/pages/admin/dashboard/dashboard.component.ts',
  'src/OpenCode.AngularFrontend/src/pages/admin/dashboard/dashboard.component.html',
]
for f in files:
    assert os.path.exists(f), f'{f} does not exist'
with open('src/OpenCode.AngularFrontend/src/pages/admin/admin-layout.component.ts') as f:
    c = f.read()
assert 'AdminLayoutComponent' in c
assert 'RouterOutlet' in c and 'RouterLink' in c
with open('src/OpenCode.AngularFrontend/src/pages/admin/dashboard/dashboard.component.ts') as f:
    c = f.read()
assert 'AdminService' in c and 'fetchStats' in c
print('Admin layout and dashboard created')
"</automated>
</verify>
<done>
- AdminLayoutComponent with dark sidebar navigation, domain sections, router-outlet
- DashboardComponent with stat cards via AdminService.fetchStats() using pageSize=1 trick
</done>
</task>

<task type="auto">
<name>Task 4: Create Angular entity admin components and register admin routes</name>
<files>
  src/OpenCode.AngularFrontend/src/pages/admin/characters/characters.component.ts
  src/OpenCode.AngularFrontend/src/pages/admin/characters/characters.component.html
  src/OpenCode.AngularFrontend/src/pages/admin/genres/genres.component.ts
  src/OpenCode.AngularFrontend/src/pages/admin/genres/genres.component.html
  src/OpenCode.AngularFrontend/src/pages/admin/artists/artists.component.ts
  src/OpenCode.AngularFrontend/src/pages/admin/artists/artists.component.html
  src/OpenCode.AngularFrontend/src/pages/admin/albums/albums.component.ts
  src/OpenCode.AngularFrontend/src/pages/admin/albums/albums.component.html
  src/OpenCode.AngularFrontend/src/pages/admin/tracks/tracks.component.ts
  src/OpenCode.AngularFrontend/src/pages/admin/tracks/tracks.component.html
  src/OpenCode.AngularFrontend/src/app/app.routes.ts
</files>
<read_first>
  src/OpenCode.AngularFrontend/src/app/app.routes.ts
  src/OpenCode.AngularFrontend/src/auth/role.guard.ts
  src/OpenCode.Frontend/src/pages/admin/CharactersPage.tsx
</read_first>
<action>
Create 5 entity admin components (matching React feature set) and update routes.

**First, create the pages/admin/ directory structure.** Each component is a standalone Angular component.

**Pattern for all entity components** (base on Characters component below):

CharactersComponent.ts:
```typescript
import { Component, inject, OnInit } from '@angular/core';
import { HttpClient } from '@angular/common/http';
import { AdminTableComponent } from '../../../shared/components/admin-table/admin-table.component';
import { ConfirmDialogComponent } from '../../../shared/components/confirm-dialog/confirm-dialog.component';

@Component({
  selector: 'app-admin-characters',
  standalone: true,
  imports: [AdminTableComponent, ConfirmDialogComponent],
  templateUrl: './characters.component.html',
})
export class CharactersComponent implements OnInit {
  private http = inject(HttpClient);
  baseUrl = 'http://localhost:8000/api/characters';
  page = 1; pageSize = 10;
  characters: any[] = [];
  totalCount = 0; totalPages = 1;
  isLoading = true;
  deleteTarget: any = null;
  isDeleting = false;
  message: { type: string; text: string } | null = null;

  columns = [
    { key: 'name', label: 'Name', sortable: true },
    { key: 'race', label: 'Race', sortable: true },
    { key: 'ki', label: 'Ki', sortable: true },
    { key: 'transformations', label: 'Transformations', render: (item: any) => String(item.transformations?.length ?? 0) },
    { key: 'planet', label: 'Planet', render: (item: any) => item.planet?.name ?? '-' },
    { key: 'createdAt', label: 'Created', sortable: true, render: (item: any) => new Date(item.createdAt).toLocaleDateString() },
    { key: 'updatedAt', label: 'Updated', sortable: true, render: (item: any) => new Date(item.updatedAt).toLocaleDateString() },
  ];

  ngOnInit(): void { this.load(); }

  load(): void {
    this.isLoading = true;
    this.http.get<any>(`${this.baseUrl}?page=${this.page}&pageSize=${this.pageSize}`)
      .subscribe({
        next: (r) => { this.characters = r.data; this.totalCount = r.totalCount; this.totalPages = r.totalPages; this.isLoading = false; },
        error: () => { this.message = { type: 'error', text: 'Failed to load characters' }; this.isLoading = false; },
      });
  }

  onPageChange(page: number): void { this.page = page; this.load(); }

  onEdit(character: any): void {
    // Navigate to character edit — use window.location or router
    window.location.href = '/dragonball';
  }

  onDelete(character: any): void { this.deleteTarget = character; }

  confirmDelete(): void {
    if (!this.deleteTarget) return;
    this.isDeleting = true;
    this.http.delete(`${this.baseUrl}/${this.deleteTarget.id}`).subscribe({
      next: () => { this.message = { type: 'success', text: 'Deleted successfully' }; this.deleteTarget = null; this.isDeleting = false; this.load(); },
      error: () => { this.message = { type: 'error', text: 'Failed to delete' }; this.isDeleting = false; },
    });
  }

  cancelDelete(): void { this.deleteTarget = null; }
  dismissMessage(): void { this.message = null; }
}
```

CharactersComponent.html:
```html
<h1 class="text-2xl font-bold mb-4">Characters ({{ totalCount }})</h1>
@if (message) {
  <div class="mb-4 px-4 py-2 rounded"
    [class.bg-green-100]="message.type === 'success'"
    [class.bg-red-100]="message.type === 'error'"
    [class.text-green-800]="message.type === 'success'"
    [class.text-red-800]="message.type === 'error'">
    {{ message.text }}
    <button (click)="dismissMessage()" class="float-right font-bold">&times;</button>
  </div>
}
<app-admin-table [columns]="columns" [data]="characters"
  [page]="page" [totalPages]="totalPages" [totalCount]="totalCount"
  [isLoading]="isLoading" [keyExtractor]="(item: any) => item.id"
  (edit)="onEdit($event)" (delete)="onDelete($event)"
  (pageChange)="onPageChange($event)">
</app-admin-table>
<app-confirm-dialog [isOpen]="!!deleteTarget" title="Delete Character"
  message="Are you sure you want to delete this character?"
  [entityName]="deleteTarget?.name ?? ''" entityType="Character"
  (confirm)="confirmDelete()" (cancel)="cancelDelete()"
  [isLoading]="isDeleting">
</app-confirm-dialog>
```

**Create remaining entity components** following the exact same pattern with entity-specific columns:

**GenresComponent:**
- Columns: name (sortable), description, createdAt (sortable, date), updatedAt (sortable, date)
- API base: `http://localhost:8000/api/genres`
- Edit navigates to genre form

**ArtistsComponent:**
- Columns: name (sortable), genre, biography (truncated to 50 chars), createdAt (sortable, date), updatedAt (sortable, date)
- API base: `http://localhost:8000/api/artists`
- Edit navigates to artist form

**AlbumsComponent:**
- Columns: title (sortable), artistName, releaseYear, genre, createdAt (sortable, date), updatedAt (sortable, date)
- API base: `http://localhost:8000/api/albums`
- Edit navigates to album form

**TracksComponent:**
- Columns: title (sortable), trackNumber, duration, albumTitle, createdAt (sortable, date), updatedAt (sortable, date)
- API base: `http://localhost:8000/api/tracks`
- Edit navigates to track form

**Update app.routes.ts** — Add admin route group with RoleGuard:

```typescript
import { Routes } from '@angular/router';

export const routes: Routes = [
  { path: '', loadComponent: () => import('../pages/dashboard/dashboard').then(m => m.DashboardComponent) },
  { path: 'login', loadComponent: () => import('../pages/login/login').then(m => m.LoginComponent) },
  { path: 'callback', loadComponent: () => import('../pages/callback/callback').then(m => m.CallbackComponent) },
  { path: 'dragonball', loadChildren: () => import('../pages/dragonball/dragonball.routes').then(m => m.routes) },
  { path: 'music', loadChildren: () => import('../pages/music/music.routes').then(m => m.routes) },
  {
    path: 'admin',
    loadComponent: () => import('../pages/admin/admin-layout.component').then(m => m.AdminLayoutComponent),
    canActivate: [RoleGuard],
    data: { roles: ['editor'] },
    children: [
      { path: '', loadComponent: () => import('../pages/admin/dashboard/dashboard.component').then(m => m.DashboardComponent) },
      { path: 'characters', loadComponent: () => import('../pages/admin/characters/characters.component').then(m => m.CharactersComponent) },
      { path: 'genres', loadComponent: () => import('../pages/admin/genres/genres.component').then(m => m.GenresComponent) },
      { path: 'artists', loadComponent: () => import('../pages/admin/artists/artists.component').then(m => m.ArtistsComponent) },
      { path: 'albums', loadComponent: () => import('../pages/admin/albums/albums.component').then(m => m.AlbumsComponent) },
      { path: 'tracks', loadComponent: () => import('../pages/admin/tracks/tracks.component').then(m => m.TracksComponent) },
    ],
  },
  { path: '**', redirectTo: '', pathMatch: 'full' },
];
```

Import RoleGuard at the top:
```typescript
import { RoleGuard } from '../auth/role.guard';
```

Note: The keyExtractor property on AdminTableComponent can't be directly bound with a lambda in Angular templates the same way as React. In the component HTML template, ensure the keyExtractor is passed as a method reference. If Angular template binding doesn't support inline functions, modify the component to use a default keyExtractor that returns `item.id`, and override per-entity via Input if needed. The simplest approach: AdminTableComponent uses `trackBy: keyExtractor` in @for or defaults to item.id. Since AdminTableComponent already defines `keyExtractor` as @Input, bind it in each page's html with `[keyExtractor]="keyExtractor"` where the component defines it:
```typescript
keyExtractor = (item: any) => item.id;
```
</action>
<verify>
  <automated>python3 -c "
import os
components = ['characters', 'genres', 'artists', 'albums', 'tracks']
for comp in components:
    ts_path = f'src/OpenCode.AngularFrontend/src/pages/admin/{comp}/{comp}.component.ts'
    html_path = f'src/OpenCode.AngularFrontend/src/pages/admin/{comp}/{comp}.component.html'
    assert os.path.exists(ts_path), f'{ts_path} does not exist'
    assert os.path.exists(html_path), f'{html_path} does not exist'

with open('src/OpenCode.AngularFrontend/src/app/app.routes.ts') as f:
    c = f.read()
assert 'path: \\'admin\\'' in c or 'path: \"admin\"' in c
assert 'RoleGuard' in c
assert \"roles: ['editor']\" in c or 'roles: [\"editor\"]' in c
assert 'admin-layout.component' in c
assert 'characters' in c and 'genres' in c and 'artists' in c
assert 'albums' in c and 'tracks' in c
print('All entity components and routes created')
"</automated>
</verify>
<done>
- 5 entity admin components (Characters, Genres, Artists, Albums, Tracks) with AdminTable, ConfirmDialog, dates-only audit columns
- Admin routes registered in app.routes.ts under /admin with RoleGuard requiring editor role
- Lazy-loaded standalone components following existing Angular patterns
</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Angular SPA → Kong Gateway | Admin CRUD operations go through same Kong gateway on port 8000 |
| Client-side route guard | RoleGuard checks editor role via decoded JWT claims |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-A01 | Spoofing | Admin route guard (RoleGuard) | mitigate | Client-side guard is UX-only; backend enforces editor role via RequireAuthorization on all POST/PUT/DELETE endpoints. RoleGuard redirects unauthorized users to `/` |
| T-12-A02 | Tampering | Delete via ConfirmDialog | mitigate | ConfirmDialog prevents accidental deletes via two-step confirmation. Actual DELETE calls require valid editor JWT |
| T-12-A03 | Information Disclosure | Dashboard stats (pageSize=1) | accept | Public GET endpoints already expose totalCount. Dashboard only shows entity counts — no PII |
| T-12-A04 | Repudiation | Dates-only audit columns | accept | Per D-06: audit is CreatedAt/UpdatedAt only (no user identity). Acceptable for PoC |
</threat_model>

<verification>
- Angular project compiles with `ng build` (or `npx ng build`) with zero errors
- AdminLayoutComponent renders with dark sidebar navigation
- DashboardComponent shows stat cards with correct entity counts
- Each entity admin component loads paginated data and supports search/sort
- Delete flow: click Delete → ConfirmDialog → confirm → entity deleted → table refreshes
- Non-editor users redirected to `/` when accessing /admin/* routes
- Feature parity with React admin panel: same admin pages, same CRUD operations, same role guard
</verification>

<success_criteria>
- AdminLayoutComponent with sidebar navigation functional across all admin routes
- AdminTableComponent with client-side search, sort, and Edit/Delete actions works for all 5 entity types
- ConfirmDialogComponent blocks delete until confirmed
- Dashboard shows entity counts fetched from existing list endpoints (pageSize=1)
- All 5 entity admin components display data, paginate, and support deletion
- CreatedAt/UpdatedAt columns visible on all entity tables (audit per D-06)
- Admin routes guarded by RoleGuard with editor role
- `ng build` passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/12-admin-panel/12-02-SUMMARY.md`
</output>
