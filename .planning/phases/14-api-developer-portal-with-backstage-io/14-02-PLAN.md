---
phase: 14-api-developer-portal-with-backstage-io
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - deploy/backstage/catalog-info.yaml
autonomous: true
requirements: [PORTAL-04, PORTAL-05, PORTAL-06]

must_haves:
  truths:
    - "Catalog YAML contains a Domain entity named opencode-platform with credentials how-to guide in its description"
    - "Catalog YAML contains two System entities: dragonball-system and music-system, both referencing opencode-platform domain"
    - "Catalog YAML contains two Component entities: opencode-dragonball-service and opencode-music-service, each referencing their system and listing providesApis"
    - "Catalog YAML contains two API entities: dragonball-api and music-api, each with $text reference to OpenAPI spec URL via gateway"
  artifacts:
    - path: "deploy/backstage/catalog-info.yaml"
      provides: "Complete catalog entity hierarchy: Domain -> Systems -> Components -> APIs with credentials guide"
      contains: "kind: Domain"
  key_links:
    - from: "deploy/backstage/catalog-info.yaml (Component.spec.providesApis)"
      to: "deploy/backstage/catalog-info.yaml (API.metadata.name)"
      via: "Backstage implicit relationship via providesApis array"
      pattern: "providesApis"
    - from: "deploy/backstage/catalog-info.yaml (System.spec.domain)"
      to: "deploy/backstage/catalog-info.yaml (Domain.metadata.name)"
      via: "Backstage implicit partOf relationship via spec.domain"
      pattern: "domain: opencode-platform"
---

<objective>
Create the complete catalog entity YAML file with Domain, Systems, Components, and API entities for the OpenCode platform, including the static credentials how-to guide.

Purpose: Per D-04, deliver the four-level entity hierarchy. Per D-06, catalog YAML lives at `deploy/backstage/catalog-info.yaml`. Per D-12, the credentials guide is embedded as a markdown description in the Domain entity. Per D-05, API entities use `$text` URLs pointing to live OpenAPI specs via the Kong gateway (using container-internal URL `gateway:8000` for Docker Compose).

Output: Single catalog YAML file with 7 entities (1 Domain, 2 Systems, 2 Components, 2 APIs).
</objective>

<execution_context>
@D:/projetos/opencodeDeepseek/.claude/get-shit-done/workflows/execute-plan.md
@D:/projetos/opencodeDeepseek/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-api-developer-portal-with-backstage-io/14-RESEARCH.md
@.planning/phases/14-api-developer-portal-with-backstage-io/14-PATTERNS.md
@.planning/phases/14-api-developer-portal-with-backstage-io/14-CONTEXT.md

<interfaces>
<!-- Catalog entity YAML pattern from existing examples -->
From src/OpenCode.Backstage/examples/entities.yaml:
```yaml
---
apiVersion: backstage.io/v1alpha1
kind: System
metadata:
  name: examples
spec:
  owner: guests
---
apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: example-website
spec:
  type: website
  lifecycle: experimental
  owner: guests
  system: examples
  providesApis: [example-grpc-api]
---
apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: example-grpc-api
spec:
  type: grpc
  lifecycle: experimental
  owner: guests
  system: examples
  definition: |
    syntax = "proto3";
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create deploy/backstage directory and catalog-info.yaml with all entities</name>
  <files>
    deploy/backstage/catalog-info.yaml
  </files>
  <read_first>
    src/OpenCode.Backstage/examples/entities.yaml
    .planning/phases/14-api-developer-portal-with-backstage-io/14-RESEARCH.md
  </read_first>
  <action>
1. Create the directory `deploy/backstage/` if it does not exist.

2. Create `deploy/backstage/catalog-info.yaml` with the following EXACT content (7 entities separated by `---`):

```yaml
---
apiVersion: backstage.io/v1alpha1
kind: Domain
metadata:
  name: opencode-platform
  description: |
    OpenCode API Platform - A proof-of-concept demonstrating .NET 10 + Aspire + Keycloak + Kong + OpenTelemetry.

    ## How to Get API Credentials

    API consumers authenticate via Keycloak OIDC. To obtain credentials:

    1. **Login to Keycloak Admin Console** at http://localhost:8080 (admin/admin)
    2. **Navigate to** Clients > Create Client
    3. **Set Client Type** to "OpenID Connect"
    4. **Enable** "Client authentication" (confidential client)
    5. **Set Access Type** to "Service Account" (client_credentials grant)
    6. **Save** and copy the Client ID and Client Secret from the Credentials tab
    7. **Request a token:**
       ```
       POST http://localhost:8080/realms/OpenCode/protocol/openid-connect/token
       Content-Type: application/x-www-form-urlencoded

       grant_type=client_credentials&client_id=YOUR_CLIENT_ID&client_secret=YOUR_SECRET
       ```
    8. **Use the token** with Kong Gateway at http://localhost:8000:
       ```
       Authorization: Bearer <access_token>
       ```

    ## Available APIs

    - **Dragon Ball API** - Character CRUD at `/api/v1/dragonball/characters`
    - **Music API** - Genre/Artist/Album/Track CRUD at `/api/v1/music/*`

    ## API Documentation

    Interactive Scalar docs are available at each API's `/scalar` endpoint.
  tags:
    - dotnet
    - rest
    - poc
spec:
  owner: guests
---
apiVersion: backstage.io/v1alpha1
kind: System
metadata:
  name: dragonball-system
  description: Dragon Ball character management API and service
  tags:
    - dotnet
    - rest
spec:
  owner: guests
  domain: opencode-platform
---
apiVersion: backstage.io/v1alpha1
kind: System
metadata:
  name: music-system
  description: Music catalog management API and service (genres, artists, albums, tracks)
  tags:
    - dotnet
    - rest
spec:
  owner: guests
  domain: opencode-platform
---
apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: opencode-dragonball-service
  description: .NET 10 Minimal API serving Dragon Ball character CRUD operations
  tags:
    - dotnet
    - minimal-api
  links:
    - url: http://localhost:5000/scalar
      title: Scalar API Docs
      icon: docs
spec:
  type: service
  lifecycle: production
  owner: guests
  system: dragonball-system
  providesApis:
    - dragonball-api
---
apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: opencode-music-service
  description: .NET 10 Minimal API serving Music catalog CRUD operations (genres, artists, albums, tracks)
  tags:
    - dotnet
    - minimal-api
  links:
    - url: http://localhost:5002/scalar
      title: Scalar API Docs
      icon: docs
spec:
  type: service
  lifecycle: production
  owner: guests
  system: music-system
  providesApis:
    - music-api
---
apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: dragonball-api
  description: Dragon Ball Character REST API - CRUD operations for characters with pagination and filtering
  tags:
    - rest
    - openapi
spec:
  type: openapi
  lifecycle: production
  owner: guests
  system: dragonball-system
  definition:
    $text: http://gateway:8000/api/v1/dragonball/openapi/v1.json
---
apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: music-api
  description: Music Catalog REST API - CRUD operations for genres, artists, albums, and tracks
  tags:
    - rest
    - openapi
spec:
  type: openapi
  lifecycle: production
  owner: guests
  system: music-system
  definition:
    $text: http://gateway:8000/api/v1/music/openapi/v1.json
```

**Entity hierarchy per D-04:**
- Domain: `opencode-platform` (top-level platform)
- Systems: `dragonball-system` and `music-system` (each with `spec.domain: opencode-platform` for implicit partOf)
- Components: `opencode-dragonball-service` and `opencode-music-service` (each with `spec.system` and `spec.providesApis`)
- APIs: `dragonball-api` and `music-api` (each with `$text` URL for live OpenAPI spec)

**API spec URLs use `http://gateway:8000/...`** (container-internal Kong hostname) because Backstage runs in Docker and needs to reach Kong on the Docker network. Per research finding, `$text` in catalog YAML does NOT support `$env{}` substitution, so URLs are hardcoded for the Docker environment. Per D-05, this is the practical resolution since env var substitution is not available in catalog YAML.

**Credentials guide (D-12):** Embedded as markdown in the Domain entity's `metadata.description` field. Backstage renders this as markdown in the entity "About" card. This is the simplest approach that requires no TechDocs pipeline (per D-08).

**Naming conventions (Claude's discretion):**
- `owner: guests` used consistently (default Backstage owner)
- `lifecycle: production` for Components and APIs (the services are deployed and functional)
- Tags include `dotnet`, `rest`, `minimal-api` for discoverability
  </action>
  <verify>
    <automated>grep -c "kind: Domain" "deploy/backstage/catalog-info.yaml" && grep -c "kind: System" "deploy/backstage/catalog-info.yaml" && grep -c "kind: Component" "deploy/backstage/catalog-info.yaml" && grep -c "kind: API" "deploy/backstage/catalog-info.yaml" && grep -c "How to Get API Credentials" "deploy/backstage/catalog-info.yaml" && grep -c "gateway:8000" "deploy/backstage/catalog-info.yaml"</automated>
  </verify>
  <acceptance_criteria>
    - File `deploy/backstage/catalog-info.yaml` exists
    - Contains exactly 1 `kind: Domain` entity with `name: opencode-platform`
    - Contains exactly 2 `kind: System` entities (`dragonball-system`, `music-system`)
    - Contains exactly 2 `kind: Component` entities (`opencode-dragonball-service`, `opencode-music-service`)
    - Contains exactly 2 `kind: API` entities (`dragonball-api`, `music-api`)
    - Domain entity description contains "How to Get API Credentials"
    - API entities contain `$text: http://gateway:8000/api/v1/dragonball/openapi/v1.json` and `$text: http://gateway:8000/api/v1/music/openapi/v1.json`
    - Both System entities have `domain: opencode-platform`
    - Both Component entities have `providesApis` arrays
    - All entities have `apiVersion: backstage.io/v1alpha1`
  </acceptance_criteria>
  <done>Complete catalog entity YAML with 7 entities (1 Domain + 2 Systems + 2 Components + 2 APIs) including credentials guide in Domain description</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Backstage Backend -> API Services | $text URL resolution fetches OpenAPI specs from API services at runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-04 | Information Disclosure | catalog-info.yaml | accept | Contains no secrets; URLs are internal Docker network addresses not accessible externally |
| T-14-05 | Tampering | $text URL resolution | accept | Low risk in PoC; $text fetches from trusted internal services on same Docker network |
</threat_model>

<verification>
1. `grep -c "kind:" deploy/backstage/catalog-info.yaml` returns 7
2. `grep "domain: opencode-platform" deploy/backstage/catalog-info.yaml` returns 2 matches (both systems)
3. `grep "providesApis" deploy/backstage/catalog-info.yaml` returns 2 matches (both components)
4. `grep "\\$text:" deploy/backstage/catalog-info.yaml` returns 2 matches (both API definitions)
</verification>

<success_criteria>
- Catalog YAML has all 7 entities in correct hierarchy
- Domain entity includes credentials how-to guide
- API entities reference live OpenAPI specs via gateway
- File uses standard Backstage catalog YAML format
</success_criteria>

<output>
After completion, create `.planning/phases/14-api-developer-portal-with-backstage-io/14-02-SUMMARY.md`
</output>
