---
phase: 14-api-developer-portal-with-backstage-io
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/OpenCode.Backstage/app-config.yaml
  - src/OpenCode.Backstage/app-config.production.yaml
  - src/OpenCode.Backstage/packages/backend/src/index.ts
  - src/OpenCode.Backstage/packages/backend/package.json
autonomous: true
requirements: [PORTAL-01, PORTAL-02, PORTAL-03]

must_haves:
  truths:
    - "Backstage backend registers OIDC auth provider instead of guest provider"
    - "app-config.yaml has full OIDC config under auth.providers.oidc.development with metadataUrl, clientId, clientSecret placeholder, tokenEndpointAuthMethod none, and signIn resolver"
    - "app-config.production.yaml has full OIDC config under auth.providers.oidc.production with env var references and no defaults"
    - "Guest auth provider is completely removed from both config files and backend code"
    - "Both config files include Domain in catalog.rules allow list"
    - "Both config files point catalog.locations to real catalog entity files instead of examples"
  artifacts:
    - path: "src/OpenCode.Backstage/app-config.yaml"
      provides: "Dev OIDC auth config + catalog rules with Domain + catalog location for real entities"
      contains: "tokenEndpointAuthMethod"
    - path: "src/OpenCode.Backstage/app-config.production.yaml"
      provides: "Production OIDC auth config + catalog rules with Domain + catalog location for container path"
      contains: "tokenEndpointAuthMethod"
    - path: "src/OpenCode.Backstage/packages/backend/src/index.ts"
      provides: "OIDC provider module registration"
      contains: "plugin-auth-backend-module-oidc-provider"
    - path: "src/OpenCode.Backstage/packages/backend/package.json"
      provides: "OIDC provider dependency"
      contains: "plugin-auth-backend-module-oidc-provider"
  key_links:
    - from: "src/OpenCode.Backstage/packages/backend/src/index.ts"
      to: "app-config.yaml auth.providers.oidc"
      via: "backend.add(import) registers OIDC module that reads config"
      pattern: "plugin-auth-backend-module-oidc-provider"
---

<objective>
Replace Backstage guest authentication with Keycloak OIDC and configure catalog rules/locations for real API entities.

Purpose: Per D-09 and D-10, developers must log in via Keycloak OIDC to access the portal. Guest auth bypass must be eliminated. Per D-07, catalog locations must point to the real entity files. Per D-04, Domain kind must be allowed in catalog rules.

Output: Four modified Backstage config/code files with OIDC auth replacing guest auth, catalog rules including Domain, and catalog locations pointing to real entity files.
</objective>

<execution_context>
@D:/projetos/opencodeDeepseek/.claude/get-shit-done/workflows/execute-plan.md
@D:/projetos/opencodeDeepseek/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-api-developer-portal-with-backstage-io/14-RESEARCH.md
@.planning/phases/14-api-developer-portal-with-backstage-io/14-PATTERNS.md
@.planning/phases/14-api-developer-portal-with-backstage-io/14-CONTEXT.md

<interfaces>
<!-- Current backend index.ts plugin registration pattern -->
From src/OpenCode.Backstage/packages/backend/src/index.ts:
```typescript
backend.add(import('@backstage/plugin-app-backend'));
backend.add(import('@backstage/plugin-proxy-backend'));
// ...
backend.add(import('@backstage/plugin-auth-backend'));
backend.add(import('@backstage/plugin-auth-backend-module-guest-provider'));
```

<!-- Current app-config.yaml env var substitution pattern -->
From src/OpenCode.Backstage/app-config.yaml:
```yaml
database:
  client: pg
  connection:
    host: ${POSTGRES_HOST:10.60.100.10}
    port: ${POSTGRES_PORT:5432}
    user: ${POSTGRES_USER:portal_user}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install OIDC provider package and swap backend module registration</name>
  <files>
    src/OpenCode.Backstage/packages/backend/package.json
    src/OpenCode.Backstage/packages/backend/src/index.ts
  </files>
  <read_first>
    src/OpenCode.Backstage/packages/backend/package.json
    src/OpenCode.Backstage/packages/backend/src/index.ts
  </read_first>
  <action>
**package.json changes:**

1. In the `dependencies` object, ADD this entry:
   ```
   "@backstage/plugin-auth-backend-module-oidc-provider": "^0.4.14"
   ```

2. REMOVE this entry:
   ```
   "@backstage/plugin-auth-backend-module-guest-provider": "^0.2.18"
   ```

Keep `@backstage/plugin-auth-backend` (line 22) -- it is the base auth plugin required by the OIDC module.

**index.ts changes:**

1. REMOVE line 29:
   ```typescript
   backend.add(import('@backstage/plugin-auth-backend-module-guest-provider'));
   ```
   Also remove the comment on line 30: `// See https://backstage.io/docs/auth/guest/provider`

2. ADD after the `backend.add(import('@backstage/plugin-auth-backend'));` line:
   ```typescript
   backend.add(import('@backstage/plugin-auth-backend-module-oidc-provider'));
   ```

3. Update the comment on the line before it to read:
   ```typescript
   // See https://backstage.io/docs/auth/oidc/ for OIDC provider configuration
   ```

Per D-09 (Keycloak OIDC login required) and D-10 (use oidc auth provider).

Do NOT run `yarn install` -- that will be done as part of the image build step (`yarn build-image`).
  </action>
  <verify>
    <automated>grep -c "plugin-auth-backend-module-oidc-provider" "src/OpenCode.Backstage/packages/backend/package.json" && grep -c "plugin-auth-backend-module-oidc-provider" "src/OpenCode.Backstage/packages/backend/src/index.ts" && grep -vc "guest-provider" "src/OpenCode.Backstage/packages/backend/src/index.ts"</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains `"@backstage/plugin-auth-backend-module-oidc-provider": "^0.4.14"`
    - package.json does NOT contain `plugin-auth-backend-module-guest-provider`
    - index.ts contains `backend.add(import('@backstage/plugin-auth-backend-module-oidc-provider'));`
    - index.ts does NOT contain `plugin-auth-backend-module-guest-provider`
    - index.ts still contains `backend.add(import('@backstage/plugin-auth-backend'));`
  </acceptance_criteria>
  <done>Backend code registers OIDC module, guest module removed from both package.json and index.ts</done>
</task>

<task type="auto">
  <name>Task 2: Replace auth and catalog config in both app-config files</name>
  <files>
    src/OpenCode.Backstage/app-config.yaml
    src/OpenCode.Backstage/app-config.production.yaml
  </files>
  <read_first>
    src/OpenCode.Backstage/app-config.yaml
    src/OpenCode.Backstage/app-config.production.yaml
    .planning/phases/14-api-developer-portal-with-backstage-io/14-RESEARCH.md
  </read_first>
  <action>
**app-config.yaml changes (dev config):**

1. REPLACE the entire `auth:` section (lines 91-111) with:
```yaml
auth:
  session:
    secret: ${AUTH_SESSION_SECRET}
  providers:
    oidc:
      development:
        metadataUrl: ${KEYCLOAK_ISSUER:http://localhost:8080/realms/OpenCode}/.well-known/openid-configuration
        clientId: ${KEYCLOAK_CLIENT_ID:backstage-portal}
        clientSecret: ${AUTH_OIDC_CLIENT_SECRET:placeholder-not-used}
        tokenEndpointAuthMethod: none
        prompt: auto
        signIn:
          resolvers:
            - resolver: emailLocalPartMatchingUserEntityName
              dangerouslyAllowSignInWithoutUserInCatalog: true
  experimentalClientIdMetadataDocuments:
    enabled: false
```

The `guest: {}` line MUST be completely removed. The `oidc:` section MUST have the `development:` environment key (Backstage reads this when NODE_ENV != production). Per D-09, D-10, D-11.

`tokenEndpointAuthMethod: none` is required because the Keycloak `backstage-portal` client is public (no client secret). The `clientSecret` placeholder is needed because Backstage schema requires it. Per research Pitfall 4.

`dangerouslyAllowSignInWithoutUserInCatalog: true` is required because no User entities exist in the catalog (PoC). Per research Pattern 1.

2. REPLACE the entire `catalog:` section (lines 116-147) with:
```yaml
catalog:
  import:
    entityFilename: catalog-info.yaml
    pullRequestBranchName: backstage-integration
  rules:
    - allow: [Component, System, API, Resource, Location, Domain]
  locations:
    - type: file
      target: ../../deploy/backstage/catalog-info.yaml
```

Key changes: Added `Domain` to allow list (per research Pitfall 2). Replaced all example locations with single real catalog file. Path `../../deploy/backstage/catalog-info.yaml` is relative to `packages/backend` (the backend process working directory). Per D-06, D-07.

**app-config.production.yaml changes:**

1. REPLACE the entire `auth:` section (lines 31-33) with:
```yaml
auth:
  session:
    secret: ${AUTH_SESSION_SECRET}
  providers:
    oidc:
      production:
        metadataUrl: ${KEYCLOAK_ISSUER}/.well-known/openid-configuration
        clientId: ${KEYCLOAK_CLIENT_ID}
        clientSecret: ${AUTH_OIDC_CLIENT_SECRET}
        tokenEndpointAuthMethod: none
        prompt: auto
        signIn:
          resolvers:
            - resolver: emailLocalPartMatchingUserEntityName
              dangerouslyAllowSignInWithoutUserInCatalog: true
```

Note: Production config uses `production:` key (not `development:`) because `NODE_ENV=production` in the Docker image. No default values on env vars -- they MUST be provided. Per D-11.

2. REPLACE the entire `catalog:` section (lines 35-55) with:
```yaml
catalog:
  rules:
    - allow: [Component, System, API, Resource, Location, Domain]
  locations:
    - type: file
      target: /app/catalog/catalog-info.yaml
```

Path `/app/catalog/catalog-info.yaml` is the container mount path where the catalog YAML will be volume-mounted. Per D-07.
  </action>
  <verify>
    <automated>grep -c "tokenEndpointAuthMethod" "src/OpenCode.Backstage/app-config.yaml" && grep -c "tokenEndpointAuthMethod" "src/OpenCode.Backstage/app-config.production.yaml" && grep -c "Domain" "src/OpenCode.Backstage/app-config.yaml" && grep -c "Domain" "src/OpenCode.Backstage/app-config.production.yaml" && grep -v "^#" "src/OpenCode.Backstage/app-config.yaml" | grep -c "guest" ; true</automated>
  </verify>
  <acceptance_criteria>
    - app-config.yaml contains `tokenEndpointAuthMethod: none` (exactly 1 occurrence)
    - app-config.yaml contains `metadataUrl: ${KEYCLOAK_ISSUER:http://localhost:8080/realms/OpenCode}/.well-known/openid-configuration`
    - app-config.yaml contains `dangerouslyAllowSignInWithoutUserInCatalog: true`
    - app-config.yaml contains `Domain` in the catalog.rules allow list
    - app-config.yaml contains `target: ../../deploy/backstage/catalog-info.yaml`
    - app-config.yaml does NOT contain `guest: {}` (zero occurrences of `guest:` as a provider)
    - app-config.production.yaml contains `tokenEndpointAuthMethod: none`
    - app-config.production.yaml contains `production:` under `oidc:` (not `development:`)
    - app-config.production.yaml contains `target: /app/catalog/catalog-info.yaml`
    - app-config.production.yaml contains `Domain` in catalog.rules
    - app-config.production.yaml does NOT contain `guest: {}`
  </acceptance_criteria>
  <done>Both config files have OIDC auth replacing guest, catalog rules include Domain, catalog locations point to real entity files</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> Backstage Backend | User authenticates via OIDC flow through Backstage backend |
| Backstage Backend -> Keycloak | Backend exchanges auth code for tokens at Keycloak token endpoint |
| Backstage Backend -> API Services | Backend fetches OpenAPI specs via $text URL resolution |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Elevation of Privilege | auth.providers | mitigate | Remove `guest: {}` entirely so OIDC is the only auth path (per D-09) |
| T-14-02 | Information Disclosure | auth.session.secret | mitigate | Use `${AUTH_SESSION_SECRET}` env var, never hardcode session secret in config files |
| T-14-03 | Spoofing | OIDC clientSecret | accept | Placeholder value used because Keycloak client is public; `tokenEndpointAuthMethod: none` prevents it from being sent |
</threat_model>

<verification>
1. `grep -c "guest" src/OpenCode.Backstage/app-config.yaml` returns 0 (or only in `spec.owner: guests` context, not as auth provider)
2. `grep -c "oidc-provider" src/OpenCode.Backstage/packages/backend/src/index.ts` returns 1
3. `grep "Domain" src/OpenCode.Backstage/app-config.yaml` shows Domain in catalog rules
4. `grep "Domain" src/OpenCode.Backstage/app-config.production.yaml` shows Domain in catalog rules
</verification>

<success_criteria>
- Guest auth completely removed from backend code and both config files
- OIDC auth configured for both development and production environments
- Catalog rules allow Domain kind in both config files
- Catalog locations point to real entity files (dev: relative path, production: container path)
</success_criteria>

<output>
After completion, create `.planning/phases/14-api-developer-portal-with-backstage-io/14-01-SUMMARY.md`
</output>
