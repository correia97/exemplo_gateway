---
phase: 08-docker-compose-production-deployment
plan: 03
type: execute
wave: 3
depends_on:
  - 08-02
files_modified:
  - .planning/phases/08-docker-compose-production-deployment/08-VERIFICATION.md
autonomous: true
requirements:
  - INFRA-04
user_setup: []
must_haves:
  truths:
    - "Verification document exists with step-by-step E2E test scenarios"
    - "Verification covers all 4 ROADMAP success criteria for Phase 8"
    - "Verification tests all 7 services start correctly via health checks"
    - "Verification tests full CRUD through Kong without Aspire"
    - "Verification tests Keycloak auth (public GET, protected POST/PUT/DELETE)"
    - "Verification tests frontend serves and SPA routing works"
    - "Verification script is copy-paste ready with expected output for each step"
    - "Troubleshooting section covers common failure modes"
  artifacts:
    - path: ".planning/phases/08-docker-compose-production-deployment/08-VERIFICATION.md"
      provides: "Complete E2E verification document for standalone Docker Compose deployment"
      contains: "docker compose up"
  key_links:
    - from: "08-VERIFICATION.md"
      to: "docker-compose.yml"
      via: "Verification step references docker compose commands"
      pattern: "docker compose"
    - from: "08-VERIFICATION.md"
      to: "Plan 05-VERIFICATION.md"
      via: "Adapts auth/route test patterns from Phase 5"
      pattern: "Authorization"
---

# 08-03: E2E Verification for Standalone Docker Compose Deployment

## Objective

Create a comprehensive verification document that proves the full stack works without Aspire orchestration. This plan produces the Phase 8 verification document (`08-VERIFICATION.md`) with step-by-step test scenarios covering the 4 ROADMAP success criteria: (1) all 7 services start, (2) full stack works without Aspire, (3) no Bitnami images, (4) health checks configured.

**Purpose:** Provide the user (and future agent sessions) a definitive way to confirm Phase 8 completeness. Without verification, there's no proof that the Docker Compose-based stack functions correctly — the entire phase goal is that `docker compose up` delivers a working system.

**Output:**
- `.planning/phases/08-docker-compose-production-deployment/08-VERIFICATION.md`

<execution_context>
@D:/projetos/opencodeDeepseek/.opencode/get-shit-done/workflows/execute-plan.md
@D:/projetos/opencodeDeepseek/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-apisix-gateway/05-VERIFICATION.md
@docker-compose.yml
@deploy/kong/config.yaml
@deploy/kong/init-routes.sh

<interfaces>
Test tokens — same pattern as Phase 5 verification:
```bash
# Get admin token for Keycloak admin operations
ADMIN_TOKEN=$(curl -s -X POST "http://localhost:8080/realms/master/protocol/openid-connect/token" \
  -H "Content-Type: application/x-www-form-urlencoded" \
  -d "username=admin&password=admin&grant_type=password&client_id=admin-cli" | \
  python3 -c "import sys,json; print(json.load(sys.stdin).get('access_token',''))")

# Get editor1 token for API write operations
EDITOR1_TOKEN=$(curl -s -X POST "http://localhost:8080/realms/OpenCode/protocol/openid-connect/token" \
  -H "Content-Type: application/x-www-form-urlencoded" \
  -d "username=editor1&password=password&grant_type=password&client_id=dragonball-api&client_secret=dragonball-secret" | \
  python3 -c "import sys,json; print(json.load(sys.stdin).get('access_token',''))")
```

Key services/ports in Docker Compose mode:
- Kong proxy: http://localhost9080
- Kong admin: http://localhost:9180
- Keycloak: http://localhost:8080
- Frontend: http://localhost:3000
- PostgreSQL: localhost:5432
</interfaces>
</context>

<tasks>

<task type="auto">
<name>Task 1: Create Phase 8 E2E verification document</name>
<files>
  .planning/phases/08-docker-compose-production-deployment/08-VERIFICATION.md
</files>
<action>
Create a comprehensive verification document at `08-VERIFICATION.md` in the Phase 8 directory. The document must cover:

1. **Prerequisites** — what must be true before testing
2. **Build phase** — building all custom images
3. **Startup verification** — all 7 services start and pass health checks
4. **Image audit** — verify no Bitnami images are used
5. **Functional tests** — Kong routing, public GET, protected write, CRUD, frontend
6. **Troubleshooting** — common failure modes and solutions

Follow the format and detail level of Phase 5's `05-VERIFICATION.md` (the most comprehensive verification doc in the project). Use PowerShell syntax for Windows compatibility (Select-String instead of grep).

<verify>
  <automated>
    test -f ".planning/phases/08-docker-compose-production-deployment/08-VERIFICATION.md" &&
    grep -q "docker compose up" ".planning/phases/08-docker-compose-production-deployment/08-VERIFICATION.md" &&
    grep -q "healthcheck" ".planning/phases/08-docker-compose-production-deployment/08-VERIFICATION.md" &&
    grep -q "dragonball" ".planning/phases/08-docker-compose-production-deployment/08-VERIFICATION.md" &&
    grep -q "music" ".planning/phases/08-docker-compose-production-deployment/08-VERIFICATION.md" &&
    grep -q "troubleshooting" ".planning/phases/08-docker-compose-production-deployment/08-VERIFICATION.md"
  </automated>
</verify>
<done>
- Verification document exists with complete E2E test scenarios
- All 7 services are tested individually (build + health check)
- All 4 ROADMAP success criteria are verified
- API functional tests cover: GET (public), POST/PUT/DELETE (protected with editor token)
- Frontend accessibility test
- No Bitnami image audit
- Troubleshooting section with common failure modes
- Document is copy-paste ready with commands and expected outputs
</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test host → Docker Compose services | Verification runs from host machine against exposed ports |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-09 | Information Disclosure | Verification tokens | accept | Tokens obtained during verification may appear in shell history; user is advised to clear history after testing. PoC scope. |
| T-08-10 | Spoofing | Test data | accept | Test data created during verification (e.g., test characters/artists) is harmless and can be deleted manually. |
</threat_model>

<verification>
1. Verification document covers all 4 ROADMAP success criteria
2. All verification commands are copy-paste ready
3. Expected outputs are documented for each test
4. Troubleshooting section addresses common failure modes
</verification>

<success_criteria>
1. `08-VERIFICATION.md` exists with complete E2E scenarios
2. Document covers: prerequisites, build phase, startup verification, image audit, functional tests, troubleshooting
3. All 7 services are individually verifyable
4. API tests cover both public and authenticated operations
5. Frontend accessibility is tested
6. Document is usable by both human operators and future agent sessions
7. Phase 8 is complete — the full stack is deployable without Aspire
</success_criteria>

<output>
After completion, create `.planning/phases/08-docker-compose-production-deployment/08-03-SUMMARY.md`
</output>
